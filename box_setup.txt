- Modify UID and GID to match LDAP entries
    - Enable root account
        sudo passwd root
    - Modify UID to 2322
        usermod -u 2322 {user}
    - Modify GID to domain_users (2000)
        groupmod -g 2000 {user}
    - Change files under /home/{user} to new uid, gid
        chown -R 2322:2000 /home/{user}
- Ensure username is as expected (mkirby)
- Install chrome
- Install vim
    sudo apt-get install vim-gtk
    # sudo apt-get install vim
- Install 'ag'
    sudo apt-get install silversearcher-ag
- Create rsa keys for git
    ssh-keygen -t rsa -b 4096 -C "michael.kirby032@gmail.com"
- Add ssh key to github
- Install vimrc
    cd ~/
    git clone --recursive https://github.com/kirby032/.vim.git
- Install dotfiles
    cd ~/
    git clone --recursive git@github.com:kirby032/dotfiles.git
- Link dotfiles as appropriate
    mv ~/.bashrc ~/.bashrc.orig
    ln -s /home/mkirby/dotfiles/.bashrc /home/mkirby/.bashrc
    mv ~/.tmux.conf ~/.tmux.conf.orig
    ln -s /home/mkirby/dotfiles/.tmux.conf /home/mkirby/.tmux.conf
    mv ~/.qumulorc ~/.qumulorc.orig
    ln -s /home/mkirby/dotfiles/.qumulorc /home/mkirby/.qumulorc
- Start ssh-agent automatically on login:
    - Copy old /usr/share/upstart/sessions/ssh-agent.conf
- Maybe switch alt and super key (if super opens activities pane you're fine)
    - Backup default mappings
        xmodmap -pke > xmodmap.defaults
    - Put following config for Apple DasKeyboard in ~/.Xmodmap
- Copy .gdbinit from last box
~~~
! swap keys {alt super}
! Actual Keyboard layout:
! keycode 64 = Super_L
! keycode 108 = Cmd_R, Super_R
! keycode 133 = Alt_L
! keycode 134 = Alt_R
! 0xcd = 205
! 0xcd = 207
clear mod4
clear mod1
keycode 64 = Super_L NoSymbol Super_L
keycode 108 = Super_R NoSymbol Super_R
keycode 133 = Alt_L Meta_L Alt_L Meta_L
keycode 134 = Alt_R Meta_R Alt_R Meta_R
add mod1 = Alt_L Alt_R Meta_L
add mod4 = Super_L Super_R Super_L Hyper_L
~~~
    - echo "/usr/bin/xmodmap /home/mkirby/.Xmodmap" > ~/.xinitrc
- Disable "Switch applications" in "Settings->Devices->Keyboard"
- Enable "Switch Windows" to be Alt+Tab in "Settings->Devices->Keyboard"
- Create hostname record in sauron
- Change hostname on box to match new record
- Modify audio output to be headphone jack and disable integrated speakers
- Install figlet
    sudo apt-get install figlet
- Install curl
    sudo apt-get install curl
- Install NFS
    sudo apt-get install nfs-common
    sudo apt-get install nfs-kernel-server
- Install Slack

//   ___                        _         ____                  _  __ _
//  / _ \ _   _ _ __ ___  _   _| | ___   / ___| _ __   ___  ___(_)/ _(_) ___
// | | | | | | | '_ ` _ \| | | | |/ _ \  \___ \| '_ \ / _ \/ __| | |_| |/ __|
// | |_| | |_| | | | | | | |_| | | (_) |  ___) | |_) |  __/ (__| |  _| | (__
//  \__\_\\__,_|_| |_| |_|\__,_|_|\___/  |____/| .__/ \___|\___|_|_| |_|\___|
//                                             |_|

- Set up dev environment
- Copy ~/.hgrc from old box
- Checkout src
    Follow http://qwiki.eng.qumulo.com/w/Manually_checking_out_source
- Add following to /etc/fstab and create target directories:
    gravytrain:/home /mnt/gravytrain/home nfs nolock 0 0
    gravytrain:/design /mnt/gravytrain/design nfs nolock 0 0
    gravytrain:/build /mnt/gravytrain/build nfs nolock 0 0
    gravytrain:/external_sources /mnt/gravytrain/external_sources nfs nolock 0 0
    gravytrain:/qumulo-dev /mnt/gravytrain/qumulo-dev nfs nolock 0 0
    gravytrain:/support /mnt/gravytrain/support nfs nolock 0 0
    gravytrain:/appliance-image /mnt/gravytrain/appliance-image nfs nolock 0 0
    gravytrain:/submit /mnt/gravytrain/submit nfs nolock 0 0
    gravytrain:/release /mnt/gravytrain/release nfs nolock 0 0
    gravytrain:/patches /mnt/gravytrain/patches nfs nolock 0 0

    iss:/build /mnt/iss/build nfs nolock 0 0
    iss:/certification /mnt/iss/certification nfs nolock 0 0

- Copy over current patch stacks to new box:
    for i in $(hg qq); do cp -R .hg/patches-$i {mounted_box_src}/.hg/; done

